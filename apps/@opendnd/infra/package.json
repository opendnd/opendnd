{
  "name": "@opendnd/infra",
  "description": "The AWS deployment as CDK: the API on Lambda behind an HTTP API, a Cognito user pool, the outbox drained onto an EventBridge bus, and a bucket for tiles and assets. No VPC, because the database is reached over TLS.",
  "scripts": {
    "build": "projen build",
    "compile": "projen compile",
    "default": "projen default",
    "deploy": "projen deploy",
    "diff": "projen diff",
    "eslint": "projen eslint",
    "package": "projen package",
    "post-compile": "projen post-compile",
    "pre-compile": "projen pre-compile",
    "synth": "projen synth",
    "test": "bun test --pass-with-no-tests --timeout 60000 ./specs",
    "watch": "projen watch",
    "projen": "projen",
    "test:watch": "bun test --watch --pass-with-no-tests --timeout 60000 ./specs",
    "test:ci": "bun test --pass-with-no-tests --timeout 60000 --reporter=junit --reporter-outfile=./test-results.xml ./specs"
  },
  "devDependencies": {
    "@types/bun": "*",
    "@types/node": "ts5.9",
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "aws-cdk": "^2.1139.0",
    "esbuild": "^0.28.2",
    "eslint": "^9",
    "eslint-config-prettier": "*",
    "eslint-import-resolver-typescript": "*",
    "eslint-plugin-import": "*",
    "eslint-plugin-prettier": "*",
    "prettier": "*",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@opendnd/api": "workspace:*",
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.8.1"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "onFail": "ignore"
    }
  },
  "main": "dist/src/index.js",
  "license": "UNLICENSED",
  "version": "0.0.0",
  "types": "dist/src/index.d.ts",
  "//": "~~ Generated by projen. To modify, edit .projenrc.js and run \"bunx projen\"."
}
