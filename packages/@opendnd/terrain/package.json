{
  "name": "@opendnd/terrain",
  "description": "A world's geography as data: reading the shapes out of a drawn map, and, from a coastline, the land beneath it. The same field however it arrived, so a world someone drew and a world nobody has drawn yet are the same kind of thing.",
  "scripts": {
    "build": "projen build",
    "compile": "projen compile",
    "default": "projen default",
    "eslint": "projen eslint",
    "package": "projen package",
    "post-compile": "projen post-compile",
    "pre-compile": "projen pre-compile",
    "test": "bun test --pass-with-no-tests --timeout 5000 ./specs",
    "watch": "projen watch",
    "projen": "projen",
    "test:watch": "bun test --watch --pass-with-no-tests --timeout 5000 ./specs",
    "test:ci": "bun test --pass-with-no-tests --timeout 5000 --reporter=junit --reporter-outfile=./test-results.xml ./specs"
  },
  "devDependencies": {
    "@opendnd/random": "workspace:*",
    "@types/bun": "*",
    "@types/node": "ts5.9",
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "eslint": "^9",
    "eslint-config-prettier": "*",
    "eslint-import-resolver-typescript": "*",
    "eslint-plugin-import": "*",
    "eslint-plugin-prettier": "*",
    "prettier": "*",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@opendnd/spatial": "workspace:*"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "onFail": "ignore"
    }
  },
  "main": "dist/src/index.js",
  "license": "UNLICENSED",
  "version": "0.0.0",
  "types": "dist/src/index.d.ts",
  "//": "~~ Generated by projen. To modify, edit .projenrc.js and run \"bunx projen\"."
}
