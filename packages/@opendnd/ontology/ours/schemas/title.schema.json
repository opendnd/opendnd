{
  "$id": "https://docs.opendnd.org/ours/schemas/title.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Title",
  "type": "object",
  "description": "A title: a seat of authority in a faction, with a rank and the law by which it passes from holder to holder.",
  "allOf": [
    {
      "$ref": "common.schema.json#/$defs/ResourceBase"
    }
  ],
  "properties": {
    "faction": {
      "$ref": "common.schema.json#/$defs/Reference",
      "description": "The faction this title belongs to."
    },
    "rank": {
      "type": "integer",
      "minimum": 0,
      "description": "Lower is higher: 0 for the sovereign seat."
    },
    "successionLaw": {
      "$ref": "../vocabularies/succession-law.schema.json"
    },
    "styleMale": {
      "type": "string",
      "description": "Form of address for a male holder, e.g. Lord."
    },
    "styleFemale": {
      "type": "string",
      "description": "Form of address for a female holder, e.g. Lady."
    },
    "styleNeuter": {
      "type": "string",
      "description": "Form of address for a holder of neither, e.g. Keeper."
    }
  },
  "required": [
    "faction",
    "successionLaw"
  ],
  "unevaluatedProperties": false
}
