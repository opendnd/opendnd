{
  "$id": "https://docs.opendnd.org/ours/schemas/world.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "World",
  "type": "object",
  "description": "A fictional universe: the root every other resource belongs to (Wikidata narrative universe).",
  "allOf": [
    {
      "$ref": "https://docs.opendnd.org/ours/schemas/common.schema.json#/$defs/ResourceBase"
    }
  ],
  "properties": {
    "calendar": {
      "$ref": "https://docs.opendnd.org/ours/schemas/common.schema.json#/$defs/Reference"
    },
    "crs": {
      "type": "string",
      "format": "uri",
      "description": "Default coordinate reference system IRI for this world's maps."
    },
    "genre": {
      "type": "string"
    },
    "summary": {
      "type": "string"
    },
    "currentTime": {
      "$ref": "https://docs.opendnd.org/ours/schemas/common.schema.json#/$defs/TemporalPosition"
    },
    "owner": {
      "type": "string",
      "description": "User id of the world owner."
    },
    "map": {
      "type": "object",
      "description": "The world's base map as picture tiles, the way web maps are served: a URL template with {z}, {x} and {y} in the web map projection, and the zooms it covers. Places are drawn over it from their cells.",
      "properties": {
        "tiles": {
          "type": "string",
          "description": "URL template for one tile, with {z}, {x} and {y} where the zoom, column and row go."
        },
        "minZoom": {
          "type": "integer",
          "minimum": 0,
          "maximum": 24,
          "description": "The coarsest zoom the tiles cover."
        },
        "maxZoom": {
          "type": "integer",
          "minimum": 0,
          "maximum": 24,
          "description": "The finest zoom the tiles cover."
        },
        "attribution": {
          "type": "string",
          "description": "Who drew the map, shown on it."
        }
      },
      "required": ["tiles"],
      "additionalProperties": false
    }
  },
  "required": [],
  "unevaluatedProperties": false
}
