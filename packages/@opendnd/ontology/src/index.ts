/**
 * @opendnd/ontology — the OpenDnD worldbuilding ontology, authored in OURS.
 *
 * This package will hold the OURS bundle (Ontology, Model and Vocabulary
 * resources), the JSON Schema each Model points at, and the alignment
 * mappings (`mapsTo`) to schema.org, Wikidata, CIDOC-CRM, OWL-Time, GEDCOM X,
 * GeoJSON and PROV-O. See ADR-002 in the docs site.
 */
export const ONTOLOGY_ID = 'https://opendnd.org/ours/ontology' as const;
