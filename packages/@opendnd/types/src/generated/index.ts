// ~~ Generated by @opendnd/ours from an OURS bundle. Do not edit by hand. ~~
// Ontology: https://opendnd.org/ours/ontology.json (version 0.1.0)

import { z } from 'zod';

/** Belief value: Whether a holder holds a proposition to be true (after CRMinf I6). */
export const beliefValueCodes = [
  "true",
  "false",
  "unknown",
] as const;
export const beliefValueSchema = z.enum(beliefValueCodes);
export type BeliefValue = z.infer<typeof beliefValueSchema>;

/** Canon status: How authoritative a record is within its world. */
export const canonStatusCodes = [
  "canon",
  "non-canon",
  "proposed",
  "generated",
  "player-authored",
  "retired",
] as const;
export const canonStatusSchema = z.enum(canonStatusCodes);
export type CanonStatus = z.infer<typeof canonStatusSchema>;

/** Event type: Kinds of event in a world's history. */
export const eventTypeCodes = [
  "birth",
  "death",
  "marriage",
  "coronation",
  "succession",
  "founding",
  "dissolution",
  "battle",
  "war",
  "treaty",
  "disaster",
  "discovery",
  "migration",
  "journey",
  "construction",
  "plague",
  "festival",
  "other",
  "session",
] as const;
export const eventTypeSchema = z.enum(eventTypeCodes);
export type EventType = z.infer<typeof eventTypeSchema>;

/** Legitimacy: Standing of a child within a line of succession. */
export const legitimacyCodes = [
  "legitimate",
  "illegitimate",
  "legitimized",
  "adopted",
] as const;
export const legitimacySchema = z.enum(legitimacyCodes);
export type Legitimacy = z.infer<typeof legitimacySchema>;

/** Name type: Which list of a culture's names to draw from. */
export const nameTypeCodes = [
  "male",
  "female",
  "neuter",
  "family",
  "place",
] as const;
export const nameTypeSchema = z.enum(nameTypeCodes);
export type NameType = z.infer<typeof nameTypeSchema>;

/** Organization type: Kinds of organized group. */
export const organizationTypeCodes = [
  "state",
  "dynasty",
  "family",
  "faction",
  "guild",
  "religion",
  "military",
  "company",
  "order",
  "school",
  "party",
] as const;
export const organizationTypeSchema = z.enum(organizationTypeCodes);
export type OrganizationType = z.infer<typeof organizationTypeSchema>;

/** Person status: Whether a person is living at the record's valid time. */
export const personStatusCodes = [
  "alive",
  "dead",
  "undead",
  "unknown",
] as const;
export const personStatusSchema = z.enum(personStatusCodes);
export type PersonStatus = z.infer<typeof personStatusSchema>;

/** Perspective: Whether a statement is made from inside the fiction or about it (after Wikidata P5102). */
export const perspectiveCodes = [
  "in-universe",
  "out-of-universe",
] as const;
export const perspectiveSchema = z.enum(perspectiveCodes);
export type Perspective = z.infer<typeof perspectiveSchema>;

/** Place type: Kinds of place, from the whole world down to a single room. */
export const placeTypeCodes = [
  "world",
  "continent",
  "region",
  "realm",
  "province",
  "city",
  "town",
  "village",
  "hamlet",
  "district",
  "building",
  "room",
  "dungeon",
  "landmark",
  "wilderness",
  "route",
  "water",
] as const;
export const placeTypeSchema = z.enum(placeTypeCodes);
export type PlaceType = z.infer<typeof placeTypeSchema>;

/** Temporal precision: The finest unit a temporal position is known to. */
export const precisionCodes = [
  "era",
  "century",
  "decade",
  "year",
  "month",
  "day",
  "hour",
  "minute",
] as const;
export const precisionSchema = z.enum(precisionCodes);
export type Precision = z.infer<typeof precisionSchema>;

/** Relationship type: Kinds of relationship between two people (GEDCOM X types plus feudal and social ties). */
export const relationshipTypeCodes = [
  "parent-child",
  "adoptive-parent-child",
  "foster-parent-child",
  "couple",
  "sibling",
  "liege-vassal",
  "mentor-student",
  "ally",
  "rival",
  "enemy",
  "friend",
] as const;
export const relationshipTypeSchema = z.enum(relationshipTypeCodes);
export type RelationshipType = z.infer<typeof relationshipTypeSchema>;

/** Sex: Biological sex as used by the genetics generator. */
export const sexCodes = [
  "male",
  "female",
  "neuter",
  "unknown",
] as const;
export const sexSchema = z.enum(sexCodes);
export type Sex = z.infer<typeof sexSchema>;

/** Creature size: Size categories for creatures, from fine to colossal. */
export const sizeCodes = [
  "fine",
  "diminutive",
  "tiny",
  "small",
  "medium",
  "large",
  "huge",
  "gargantuan",
  "colossal",
] as const;
export const sizeSchema = z.enum(sizeCodes);
export type Size = z.infer<typeof sizeSchema>;

/** Work type: Kinds of in-world or out-of-world creative work. */
export const workTypeCodes = [
  "chronicle",
  "legend",
  "myth",
  "song",
  "letter",
  "report",
  "map",
  "article",
  "adventure",
  "rulebook",
] as const;
export const workTypeSchema = z.enum(workTypeCodes);
export type WorkType = z.infer<typeof workTypeSchema>;

/** A typed pointer to another resource in the same world. */
export const referenceSchema = z.strictObject({
  /** Model id of the target, e.g. person. */
  model: z.string(),
  id: z.uuid(),
  /** Denormalized display name, for convenience. */
  name: z.string().optional(),
});
export type Reference = z.infer<typeof referenceSchema>;

/** Where a fact is attested inside the fiction: a Work and a locator within it. */
export const citationSchema = z.strictObject({
  work: referenceSchema,
  /** Page, chapter, verse or similar. */
  locator: z.string().optional(),
  quote: z.string().optional(),
});
export type Citation = z.infer<typeof citationSchema>;

/** A GeoJSON position: [x, y] or [x, y, z] in the world's CRS. */
export const positionSchema = z.array(z.number()).min(2).max(3);
export type Position = z.infer<typeof positionSchema>;

/** A GeoJSON geometry object. */
export const geometrySchema = z.union([
  z.strictObject({
    type: z.literal("Point"),
    coordinates: positionSchema,
  }),
  z.strictObject({
    type: z.literal("LineString"),
    coordinates: z.array(positionSchema).min(2),
  }),
  z.strictObject({
    type: z.literal("Polygon"),
    coordinates: z.array(z.array(positionSchema).min(4)),
  }),
  z.strictObject({
    type: z.literal("MultiPolygon"),
    coordinates: z.array(z.array(z.array(positionSchema).min(4))),
  }),
]);
export type Geometry = z.infer<typeof geometrySchema>;

/** Geometry placed in a named coordinate reference system (GeoSPARQL Feature/Geometry split). GeoJSON hard-codes Earth, so the CRS travels beside the geometry. */
export const featureSchema = z.strictObject({
  /** IRI of the world's CRS, minted per world. */
  crs: z.url(),
  geometry: geometrySchema,
  properties: z.record(z.string(), z.unknown()).optional(),
});
export type Feature = z.infer<typeof featureSchema>;

/** How this record came to exist (PROV-O), including the generator seed when it was produced procedurally or by AI. */
export const provenanceSchema = z.strictObject({
  /** Generator id and version, e.g. dynastia@2.0.0 or bedrock:anthropic.claude-sonnet-5. */
  generatedBy: z.string().optional(),
  /** Deterministic seed path. Same seed and generator yields the same derivedId. */
  seed: z.string().optional(),
  parameters: z.record(z.string(), z.unknown()).optional(),
  /** Hash of the prompt for AI-generated records. */
  promptHash: z.string().optional(),
  derivedFrom: z.array(referenceSchema).optional(),
  /** User id of the human author or approver. */
  attributedTo: z.string().optional(),
  /** The record this one supersedes. */
  revisionOf: z.uuid().optional(),
});
export type Provenance = z.infer<typeof provenanceSchema>;

/** Transaction time: when the record itself was written, as opposed to when the fact holds in-world. */
export const recordedSchema = z.strictObject({
  createdAt: z.iso.datetime(),
  updatedAt: z.iso.datetime(),
  revision: z.int().min(1),
  /** User id. */
  author: z.string().optional(),
});
export type Recorded = z.infer<typeof recordedSchema>;

/** A point in in-world time, expressed in a named calendar (OWL-Time TemporalPosition with a temporal reference system). */
export const temporalPositionSchema = z.strictObject({
  /** The Calendar resource this position is expressed in. */
  trs: z.uuid(),
  year: z.int(),
  month: z.int().min(1).optional(),
  day: z.int().min(1).optional(),
  hour: z.int().min(0).max(23).optional(),
  minute: z.int().min(0).max(59).optional(),
  precision: precisionSchema.default("year"),
  /** A named position when no numeric one is known, e.g. 'the Age of Ash'. */
  nominal: z.string().optional(),
});
export type TemporalPosition = z.infer<typeof temporalPositionSchema>;

/** An interval of in-world time with optional fuzzy bounds (CIDOC-CRM E52 Time-Span). */
export const timeSpanSchema = z.strictObject({
  begin: temporalPositionSchema.optional(),
  end: temporalPositionSchema.optional(),
  /** Outer lower bound when begin is uncertain. */
  earliest: temporalPositionSchema.optional(),
  /** Outer upper bound when end is uncertain. */
  latest: temporalPositionSchema.optional(),
});
export type TimeSpan = z.infer<typeof timeSpanSchema>;

/** Every OpenDnD resource is an assertion about a world: it carries in-world valid time, transaction time, canon status, perspective and provenance. */
export const resourceBaseSchema = z.object({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
});
export type ResourceBase = z.infer<typeof resourceBaseSchema>;

/** A temporal reference system: months, weekdays, leap rules, moons and eras (after Kanka; OWL-Time TRS). */
export const calendarSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  months: z.array(z.strictObject({
    name: z.string(),
    length: z.int().min(1),
    intercalary: z.boolean().default(false),
  })).min(1),
  weekdays: z.array(z.string()).optional(),
  leapYear: z.strictObject({
    every: z.int().min(1),
    extraDays: z.int().min(1),
    month: z.int().min(1),
    offset: z.int().default(0),
  }).optional(),
  moons: z.array(z.strictObject({
    name: z.string(),
    /** Days per full cycle. */
    cycle: z.number(),
    offset: z.number().default(0),
  })).optional(),
  seasons: z.array(z.strictObject({
    name: z.string(),
    month: z.int().min(1),
    day: z.int().min(1),
  })).optional(),
  eras: z.array(z.strictObject({
    name: z.string(),
    abbreviation: z.string().optional(),
    startYear: z.int(),
    endYear: z.int().optional(),
  })).optional(),
  hasYearZero: z.boolean().default(false),
  yearSuffix: z.string().optional(),
});
export type Calendar = z.infer<typeof calendarSchema>;

/** A belief held by a person or organization about a proposition, so contested history can be recorded without choosing a winner (CRMinf Belief). */
export const claimSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  holder: referenceSchema,
  proposition: z.string(),
  about: z.array(referenceSchema).optional(),
  value: beliefValueSchema,
  /** Why the holder believes it: observation, inference, tradition, adoption. */
  basis: z.string().optional(),
});
export type Claim = z.infer<typeof claimSchema>;

/** A people's shared naming, language and customs, separate from biology so one species can hold many cultures and one culture span species (World Anvil Ethnicity). */
export const cultureSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  /** Seed lists the name generator learns from, by name type. */
  names: z.strictObject({
    male: z.array(z.string()).optional(),
    female: z.array(z.string()).optional(),
    neuter: z.array(z.string()).optional(),
    family: z.array(z.string()).optional(),
    place: z.array(z.string()).optional(),
  }).optional(),
  languages: z.array(referenceSchema).optional(),
  /** Species that commonly belong to this culture. */
  species: z.array(referenceSchema).optional(),
  /** Prose on how names are formed and used. */
  namingConventions: z.string().optional(),
});
export type Culture = z.infer<typeof cultureSchema>;

/** Something that happened in-world, with participants in roles and optional cause links (schema.org Event, CIDOC E5, Dwarf Fortress historical events). */
export const eventSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  eventType: eventTypeSchema,
  when: timeSpanSchema,
  locations: z.array(referenceSchema).optional(),
  participants: z.array(z.strictObject({
    actor: referenceSchema,
    role: z.string(),
  })).optional(),
  partOf: referenceSchema.optional(),
  causedBy: z.array(referenceSchema).optional(),
  outcome: z.string().optional(),
});
export type Event = z.infer<typeof eventSchema>;

/** An organized group: state, dynasty, faction, guild, religion (schema.org Organization, CIDOC E74 Group, W3C ORG). */
export const organizationSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  organizationType: organizationTypeSchema,
  parent: referenceSchema.optional(),
  seat: referenceSchema.optional(),
  leader: referenceSchema.optional(),
  founded: temporalPositionSchema.optional(),
  dissolved: temporalPositionSchema.optional(),
  motto: z.string().optional(),
  sigil: z.url().optional(),
});
export type Organization = z.infer<typeof organizationSchema>;

/** A person in the world, real to it or legendary (schema.org Person, CIDOC E21, Wikidata fictional human). */
export const personSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  species: referenceSchema.optional(),
  culture: referenceSchema.optional(),
  sex: sexSchema.optional(),
  pronouns: z.string().optional(),
  status: personStatusSchema.default("alive"),
  birth: z.strictObject({
    time: temporalPositionSchema.optional(),
    place: referenceSchema.optional(),
  }).optional(),
  death: z.strictObject({
    time: temporalPositionSchema.optional(),
    place: referenceSchema.optional(),
    cause: z.string().optional(),
  }).optional(),
  titles: z.array(z.string()).optional(),
  memberOf: z.array(referenceSchema).optional(),
  residence: referenceSchema.optional(),
  occupation: z.string().optional(),
  /** Genetic record produced by the genetics generator. */
  genome: z.strictObject({
    /** Chromosome number to allele pair, e.g. '3=9' or 'X1=Y3'. */
    chromosomes: z.record(z.string(), z.string().regex(new RegExp("^[XY]?[0-9]+=[XY]?[0-9]+$"))),
    /** Inches. */
    height: z.int().optional(),
    /** Pounds. */
    weight: z.int().optional(),
  }).optional(),
  traits: z.array(z.strictObject({
    gene: z.string(),
    trait: z.string(),
  })).optional(),
  portrait: z.url().optional(),
});
export type Person = z.infer<typeof personSchema>;

/** A location at any scale, optionally with geometry in the world's CRS (schema.org Place, CIDOC E53, GeoSPARQL Feature). */
export const placeSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  placeType: placeTypeSchema,
  parent: referenceSchema.optional(),
  feature: featureSchema.optional(),
  /** Quadtree cell token for this place at its own scale (a realm at a coarse level, a building at a fine one). Containment is a prefix relationship. See ADR-006. */
  cell: z.string().regex(new RegExp("^[0-9a-f]{1,16}$")).optional(),
  population: z.int().min(0).optional(),
  biome: z.string().optional(),
  controlledBy: referenceSchema.optional(),
  founded: temporalPositionSchema.optional(),
});
export type Place = z.infer<typeof placeSchema>;

/** A tie between two people, with dated facts (GEDCOM X Relationship extended for succession). */
export const relationshipSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  relationshipType: relationshipTypeSchema,
  person1: referenceSchema,
  person2: referenceSchema,
  facts: z.array(z.strictObject({
    type: z.string(),
    time: temporalPositionSchema.optional(),
    place: referenceSchema.optional(),
  })).optional(),
  legitimacy: legitimacySchema.optional(),
  successionOrder: z.int().min(1).optional(),
});
export type Relationship = z.infer<typeof relationshipSchema>;

/** A kind of creature and its biology: size, chromosome layout, trait dictionary, growth and lifespan tables. Rules content (abilities, features) lives in the rules layer, not here (World Anvil Species; schema.org Taxon). */
export const speciesSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  size: sizeSchema,
  /** The species this is a subspecies of. */
  parent: referenceSchema.optional(),
  /** Chromosome number to the die rolled for each allele, or 'sex' for the sex chromosome. */
  chromosomes: z.record(z.string(), z.string().regex(new RegExp("^(d[0-9]+|sex)$"))).optional(),
  /** Dice rolled for the X and Y alleles of the sex chromosome. */
  sexChromosomes: z.strictObject({
    /** A die such as d8. */
    x: z.string().regex(new RegExp("^d[0-9]+$")),
    /** A die such as d8. */
    y: z.string().regex(new RegExp("^d[0-9]+$")),
  }).optional(),
  /** Trait category (eyeColor, hairColor, ...) to the chromosome number that expresses it. */
  categories: z.record(z.string(), z.string()).optional(),
  /** Gene key to trait. Common genes are '<category>:C<chromosome>:<dominant roll>'; rare genes are '<category>:C<chromosome>:<rollA>=<rollB>'. */
  traitDictionary: z.record(z.string(), z.string()).optional(),
  height: z.strictObject({
    /** Base height in inches. */
    base: z.int(),
    /** Height modifier dice; the total is added to base. */
    dice: z.array(z.string().regex(new RegExp("^d[0-9]+$"))).min(1),
  }).optional(),
  weight: z.strictObject({
    /** Base weight in pounds. */
    base: z.int(),
    /** Weight modifier dice, multiplied by the height modifier. */
    dice: z.array(z.string().regex(new RegExp("^d[0-9]+$"))).optional(),
    /** Fixed multiplier used instead of dice. */
    multiplier: z.number().optional(),
  }).optional(),
  /** Named age groups with their year bounds and generation dice. */
  ageRanges: z.record(z.string(), z.strictObject({
    min: z.int(),
    max: z.int(),
    /** Relative likelihood of this age group. */
    weight: z.number(),
    dice: z.array(z.string().regex(new RegExp("^d[0-9]+$"))).optional(),
  })).optional(),
  /** Walking speed in feet. */
  speed: z.int().min(0).optional(),
});
export type Species = z.infer<typeof speciesSchema>;

/** A creative work, in-world (a chronicle, a legend) or out-of-world (an adventure module) (schema.org CreativeWork; NOnt narration). */
export const workSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  workType: workTypeSchema,
  author: referenceSchema.optional(),
  about: z.array(referenceSchema).optional(),
  text: z.string().optional(),
  language: z.string().optional(),
});
export type Work = z.infer<typeof workSchema>;

/** A fictional universe: the root every other resource belongs to (Wikidata narrative universe). */
export const worldSchema = z.strictObject({
  /** Random v4. Never derived from mutable content, so renames never break references. */
  id: z.uuid(),
  /** Deterministic v5 id derived from the provenance seed, so regeneration is idempotent. */
  derivedId: z.uuid().optional(),
  /** The World this resource belongs to. */
  world: z.uuid(),
  name: z.string().min(1),
  alternateNames: z.array(z.string()).optional(),
  description: z.string().optional(),
  canonStatus: canonStatusSchema,
  perspective: perspectiveSchema.default("in-universe"),
  /** When this assertion holds in-world. Absent means always. */
  validTime: timeSpanSchema.optional(),
  recorded: recordedSchema,
  provenance: provenanceSchema.optional(),
  citations: z.array(citationSchema).optional(),
  tags: z.array(z.string()).optional(),
  /** Content-addressed id of the module this record ships in, when it is not native to the world. */
  module: z.string().optional(),
  calendar: referenceSchema.optional(),
  /** Default coordinate reference system IRI for this world's maps. */
  crs: z.url().optional(),
  genre: z.string().optional(),
  summary: z.string().optional(),
  currentTime: temporalPositionSchema.optional(),
  /** User id of the world owner. */
  owner: z.string().optional(),
});
export type World = z.infer<typeof worldSchema>;

/** Every model in the ontology, keyed by model id. */
export const models = {
  calendar: calendarSchema,
  claim: claimSchema,
  culture: cultureSchema,
  event: eventSchema,
  organization: organizationSchema,
  person: personSchema,
  place: placeSchema,
  relationship: relationshipSchema,
  species: speciesSchema,
  work: workSchema,
  world: worldSchema,
} as const;
export type ModelId = keyof typeof models;
